; Test file for label definitions
; This file contains various label definitions to test the parser

; Simple labels
start:
main:
loop:

; Labels with comments
init:       ; Initialize
process:    ; Process data
cleanup:    ; Clean up resources

; Multiple labels
done:
end:
finish:
